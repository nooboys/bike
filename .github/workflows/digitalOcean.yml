name: Deploy to DigitalOcean

on:
  push:
    branches:
      - master
  workflow_dispatch: null   

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Copy files to server
      run: sshpass -p ${{ Bharat@123Kota }} scp -r ./* ${{ root }}@${{ 209.38.149.108 }}:/var/www
      env:
        SSHPASS: ${{ Bharat@123Kota }}

    - name: Deploy on server
      run: |
        sshpass -p ${{ secrets.DO_DROPLET_PASSWORD }} ssh ${{ secrets.DO_DROPLET_USER }}@${{ secrets.DO_DROPLET_IP }} << 'EOF'
        cd /var/www
        ls
        pwd
        EOF
      env:
        SSHPASS: ${{ Bharat@123Kota }}
